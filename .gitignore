.claude/
CLAUDE.md
docs/

# Compiled binaries
*.exe
library-server
library-server~

# SQLite runtime files
*.db
*.db-shm
*.db-wal

# Generated runtime directories
logs/
uploads/

# Server config — holds live credentials (admin password, mcp_token,
# deepfreeze password). Never track; server auto-generates a safe default
# (all secrets blank) on first run if the file is missing — see
# server/config.go's defaultConfigYAML.
config.yaml
server/config.yaml

# Veyon sync — credentials + generated CSV/log output
veyon_sync_config.json
veyon_export/
__pycache__/
ready-publish-plan.md
DEPLOY.md
ambil_file2.bat
PATCH-AGENT.md
plugin-plan.md
plan-pending-command.md
copy.ps1
note.md
index.html
kirim_file.bat
kirim_file.log
update_server.bat
veyon_sync_config.json
veyon_sync_config.example.json
veyon_sync.py
SESSION_MEMORY.md